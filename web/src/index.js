import React from 'react';
import ReactDOM from 'react-dom'; // O React DOM é apenas para interação com web
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

// JSX = Javascript + XML (HTML)